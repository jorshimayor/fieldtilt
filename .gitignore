node_modules
dist
coverage

# --- secrets ---
# Never commit any .env variant. .env.example IS tracked (see below).
.env
.env.*
!.env.example

# --- vercel local state ---
.vercel
.vercel-env.json

# --- editor / os ---
.DS_Store
*.log
.env*.local
.env*

# wrangler auth/config home (contains OAuth tokens — never commit)
.wrangler-xdg/

# Private source-of-truth docs (career plan, season calendar) — NEVER commit
docs/
.wrangler/
